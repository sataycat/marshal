{
  "name": "marshal-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && size-limit",
    "size": "size-limit",
    "analyze": "cross-env ANALYZE=1 vite build && size-limit",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "marked": "^15.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "wouter": "^3.10.0"
  },
  "devDependencies": {
    "@size-limit/preset-app": "^12.1.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "cross-env": "^10.1.0",
    "rollup-plugin-visualizer": "^7.0.1",
    "size-limit": "^12.1.0",
    "typescript": "^6.0.3",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
